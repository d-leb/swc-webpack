/*

This is a webpack config file that is used by swc-webpack to analyze the production build. It is applied over the production
and base configs.

The existing config is commented out below for reference. Edit the configuration to suit your needs.

*/

export default () => ({
  /*
  plugins: [new BundleAnalyzerPlugin()],
  */
})
