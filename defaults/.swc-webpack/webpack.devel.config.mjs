/*

This is a webpack config file that is used by swc-webpack in the development environment. It is applied over the base config.

The existing config is commented out below for reference. Edit the configuration to suit your needs.

*/

export default () => ({
  /*
  mode: 'development',
  devtool: 'eval-cheap-module-source-map',
  output: {
    publicPath: '/',
  },
  stats: 'errors-warnings',
  devServer: {
    client: {
      overlay: {
        errors: true,
        warnings: true,
      },
    },
    historyApiFallback: true,
    host: 'localhost',
    hot: true,
    port: 3000,
    open: true,
  },*/
})
